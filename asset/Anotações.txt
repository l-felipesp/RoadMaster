Entity
        Score = None
        Name = name
        Surface = pygame.image.load('./asset/' + name + '.png').convert_alpha()
        Rect = self.surf.get_rect(left=position[0], top=position[1])
        Speed = 0
        Health = ENTITY_HEALTH[self.name]

Player - Deriva da Classe abstrata Entity
- Posição inicial - Definida no match da EntityFactory
- Saúde - Definida em entity.py_Entity(ABC) usando uma variável do arquivo const.py
- Movimentação - função move descrita no arquivo player.py
	Talvez aqui eu deva implementar ifs usando variáveis booleanas de colisão
- Velocidade - variável da classe abstrata Entity usando uma variável do arquivo const.py

Inimigo - Carros que vão (enemyForward.py_EnemyForward) - Deriva da Classe abstrata Entity
- Posição inicial - random entre faixa 1 e faixa 2
- Aparência - DEFINIR UM RANDOM PARA ISSO TAMBÉM
- Movimentação - TENTAR DEFINIR UMA MOVIMENTAÇÃO RANDOM ENTRE VÁRIAS PRÉ-DETERMINADAS (mudar de faixa, etc)

Inimigo - Carros que vêm (enemyReverse.py_EnemyReverse) - Deriva da Classe abstrata Entity
- Posição inicial - random entre faixa 1 e faixa 2
- Aparência - DEFINIR UM RANDOM PARA ISSO TAMBÉM
- Movimentação - TENTAR DEFINIR UMA MOVIMENTAÇÃO RANDOM ENTRE VÁRIAS PRÉ-DETERMINADAS (mudar de faixa, etc)
- Definir colisão

Pontuação - Acho que convém criar uma entity para isso
- Posição inicial - Será fixa
- Funcionamento - Variará (mais pontos andando na contra-mão e andando mais próximo da direita)


Seu jogo precisa ter um menu e já na tela do menu você deve escrever em alguma
parte dele os comandos de controle. Por exemplo: “Space – Saltar”. Ou “CTRL – Atirar” etc.